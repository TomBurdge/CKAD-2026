apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: broken-app
  namespace: q08
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: web
          image: nginx
