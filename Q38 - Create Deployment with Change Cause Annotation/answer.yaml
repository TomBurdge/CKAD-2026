apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tracked-app
  annotations:
    kubernetes.io/change-cause: "Initial deployment with nginx:1.20"
  name: tracked-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tracked-app
  strategy: {}
  template:
    metadata:
      labels:
        app: tracked-app
    spec:
      containers:
      - image: nginx:1.20
        name: nginx
        resources: {}
status: {}
